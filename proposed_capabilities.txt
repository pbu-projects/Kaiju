|*Proposed Capability* |Rebuild location autocomplete on a geocoding
provider/config that is genuinely country-aware, tested against all 17
supported countries' postal formats, not just US ZIP codes.
--
|*Proposed Capability* |Introduce a single, tested geo-scoping service
used consistently by banners, carousels, and "regional" project
matching, instead of each surface implementing its own (evidently
--
|*Proposed Capability* |Re-implement default sort as a verified
nearest-first ranking, and fix regional-project geometry so a regional
project's catchment area is evaluated correctly against every search
--
|*Proposed Capability* |Rebuild the search index/materialized view so it
is driven directly off the same project-state transitions as the admin
and detail-page views, eliminating the current dual-source-of-truth
--
|*Proposed Capability* |Audit and fix filter-to-query wiring for every
filter in the panel (Section 4 <<CUR-003>>), with regression tests per
filter.
--
|*Proposed Capability* |Harden result-card rendering and the map-link
generator against edge-case data (accents, punctuation,
zero-remaining-capacity, corrected addresses), and make the displayed
--
|*Proposed Capability* |Rebuild the wizard's save/submit path with
per-field, per-step validation and specific, actionable error messages
(never a bare "application error"); make every state-changing action
--
|*Proposed Capability* |Rebuild recurrence as a single well-tested
scheduling model (occurrence generation, editing, timezone handling,
capacity per occurrence) shared by the wizard, the project detail page,
--
|*Proposed Capability* |Replace the wizard's location step with a
single, tested address/geocoding component (map, autocomplete,
regional-boundary editor, coordinate entry) shared with search (<<ENH-001>>)
--
|*Proposed Capability* |Rebuild templates to snapshot the complete
project data model, and ensure a project instantiated from a template is
indistinguishable (in editability and dashboard visibility) from one
--
|*Proposed Capability* |Fix org-search indexing/query parity with the
main organization directory (<<CUR-007>>), make org (de)selection state
fully deterministic, and size the results panel to its content.
--
|*Proposed Capability* |Adopt one validation pattern used consistently
across every wizard step: inline, field-level errors that state exactly
what's wrong and how to fix it, with visual required/optional indicators
--
|*Proposed Capability* |Rebuild image/attachment upload, storage, and
removal as one reliable component with filename sanitization and a save
path that can't silently fail.
--
|*Proposed Capability* |Run a copy and visual-QA pass against the design
spec as part of the rebuild's definition of done for the wizard, rather
than as a follow-up.
--
|*Proposed Capability* |Make capacity enforcement a single server-side
check applied uniformly at sign-up, independent of client (web/mobile)
or entry point.
--
|*Proposed Capability* |Make the draft/published boundary a real,
enforced state (nothing volunteer-facing changes until Publish is
clicked) and only show the "changes will be lost" warning when the form
--
|*Proposed Capability* |Let external-link projects route straight
through without requiring JustServe sign-in, since the actual commitment
is captured on the third-party site, not on JustServe.
--
|*Proposed Capability* |Add automated pre-screening (keyword/pattern
rules at minimum) at submission time, ahead of and in addition to human
specialist review (<<CUR-018>>).
--
|*Proposed Capability* |Confirm <<CUR-016>> (project editing) covers every
field an organizer would reasonably need to change post-publish, closing
whatever gap currently forces a support ticket.
--
|*Proposed Capability* |Fix the submitter/owner data model so it never
falls back to a placeholder or an unrelated signed-in user's data,
correct the volunteer-count calculation, and give sponsors a dedicated
--
|*Proposed Capability* |Remove the artificial Favorites display cap
(with pagination if needed) and fix the unvolunteer action to reliably
cancel the commitment (<<CUR-021>>).
--
|*Proposed Capability* |Rebuild org search/linking on the same reliable
component proposed in <<ENH-011>>, shared between the project wizard and the
standalone organization directory (<<CUR-007>>).
--
|*Proposed Capability* |Fix each interaction as part of a single
organization-page component rebuild (<<CUR-028>>, <<CUR-031>>), and add a
pre-publish content-QA check to prevent placeholder text reaching
--
|*Proposed Capability* |Fix the announcement save path and correct the
permission check that's blocking valid in-jurisdiction specialist
access.
--
|*Proposed Capability* |Build a proper admin UI for VC promotion
(replacing the current API-only path), harden the Give-page link field
against the failing inputs, and ensure every VC page has a safe default
--
|*Proposed Capability* |Fix the email template's Accept/Decline button
styling, and extend the endorsement model (<<CUR-029>>) to support multiple
organizations formally co-sponsoring a single project.
--
|*Proposed Capability* |Treat identity/SSO as the rebuild's highest-risk
integration: a full audit of the Okta-backed identity flows (<<CUR-034>>)
against every supported provider, explicit and consistent
--
|*Proposed Capability* |Fix the JustServe-to-Zendesk SSO handoff as part
of the retained Zendesk integration (<<CUR-065>>), since Help Center access
depends on it.
--
|*Proposed Capability* |Rebuild registration/profile field logic as an
explicit, tested dependency chain (country determines postal format and
state/province requirement) with inline guidance, especially for the
--
|*Proposed Capability* |Ensure session/account context is deterministic
and never silently swaps identities mid-session; add hard validation
before any content-creation action to confirm which account is active.
--
|*Proposed Capability* |Tie the "unsaved changes" warning strictly to
actual unsaved form state, and make account deletion an unambiguous,
immediately-final action with a clear confirmation and landing page.
--
|*Proposed Capability* |Ensure every registration failure path (<<CUR-033>>)
surfaces a clear, user-facing message and next step, never a raw
upstream error or silent no-op.
--
|*Proposed Capability* |Rebuild admin-initiated password actions
(<<CUR-044>>) to both succeed reliably within the admin's scope and to
report actual, verified outcomes rather than optimistic success
--
|*Proposed Capability* |Audit and correct the full civic and
ecclesiastical reference datasets as a data-migration task ahead of
launch, with an ongoing correction workflow for the operator's local
--
|*Proposed Capability* |Rebuild admin search on a single, well-tested
query engine shared across Manage Users/Organizations/Projects, with an
explicit, terminating "no results" state.
--
|*Proposed Capability* |Model project ownership and reassignment as a
single authoritative state machine (<<CUR-041>>) with an audit trail,
replacing what currently behaves as several inconsistent,
--
|*Proposed Capability* |Rebuild the admin tree as a single accurate
read-model over the boundary/role data (<<CUR-039>>, <<CUR-040>>), with the same
view guaranteed for every viewer who has rights to see a given node.
--
|*Proposed Capability* |Treat this as a security-relevant permissions
rebuild, not just a bug list: re-derive the approval-routing and
edit-permission logic directly and exclusively from the boundary/role
--
|*Proposed Capability* |Rebuild Manage Projects as the single, complete
admin view of every in-scope project (replacing the need to fall back to
the legacy screen), with each filter/search path covered by its own
--
|*Proposed Capability* |Rebuild Manage/Email Volunteers as a single
shift-aware component (replacing the deprecated page entirely) that
always shows which shift a volunteer belongs to.
--
|*Proposed Capability* |Include these in the admin-page component
rebuild (<<CUR-042>>/<<CUR-040>> UI) with scroll-position preservation as an
explicit requirement for any action taken from within a results list.
--
|*Proposed Capability* |Run a one-time data-remediation pass identifying
and correcting corrupted date fields as part of migration, and add
server-side range validation going forward so this class of corruption
--
|*Proposed Capability* |Rebuild metrics (<<CUR-045>>) on a verified, tested
aggregation pipeline with a defined, short maximum lag between a unit's
creation and its first reportable data.
--
|*Proposed Capability* |Treat permission/rights assignment as a
transactional operation with confirmation and rollback on failure, and
add a regression test suite specifically to prevent previously-fixed
--
|*Proposed Capability* |Rebuild notifications (<<CUR-046>>, <<CUR-047>>) as an
explicit, tested event-to-recipient mapping - one entry per lifecycle
transition and recipient role - rather than notification logic implicit
--
|*Proposed Capability* |Filter notification content against current,
live project state at send time (not at the time the notification was
queued), always include a deep link, and gate country/region-targeted
--
|*Proposed Capability* |Build a proper branded 404 page (net new) and a
sitewide error-boundary pattern that always shows a specific, actionable
message instead of a generic exception dump.
--
|*Proposed Capability* |Fix the mismatch between the field's displayed
character limit and its actual server-side acceptance limit, and add a
regression test given this has already recurred once.
--
|*Proposed Capability* |Rebuild the Cities directory (<<CUR-055>>) as a
properly paginated or virtualized list that is fully
scrollable/reachable at any viewport size.
--
|*Proposed Capability* |Put static/CMS content under the same editorial
review cadence as code changes, with a lightweight periodic staleness
check (especially for date-referencing content).
--
|*Proposed Capability* |Write and surface upload-specific terms (e.g.,
image rights/consent, tying naturally to ENH's GDPR item below) rather
than reusing an unrelated general T&C block.
--
|*Proposed Capability* |Rebuild success-story search on the same
geo/ranking foundation proposed for project search (<<ENH-003>>), including
correct default geo-scoping for signed-out visitors.
--
|*Proposed Capability* |Rebuild success-story authoring as part of the
same content/attachment pipeline hardened for projects (<<ENH-013>>),
including a fully validated, editable location field.
--
|*Proposed Capability* |Fix the "My Stories" ownership query, and add a
graceful fallback image (not blank/plain text) for any story missing a
renderable thumbnail.
--
|*Proposed Capability* |Rebuild the page shell (header/nav/footer) as
one component instance used everywhere with no per-page divergence, and
include modal/dialog sizing in the design system's own component tests.
--
|*Proposed Capability* |Ensure Open Graph metadata (<<CUR-066>>) regenerates
whenever a project is edited, and validate image URLs are always
publicly resolvable at share time.
--
|*Proposed Capability* |Ensure the language switcher (<<CUR-058>>) is wired
identically across every authenticated surface, not only public pages.

--
|*Proposed Capability* |Move all user-facing strings (UI, email
templates, and admin screens alike) into a single translation-managed
string catalog with a completeness check as part of the build/release
--
|*Proposed Capability* |Drive date and unit formatting off the resolved
locale (not just language) consistently for both signed-in and
signed-out users (<<CUR-060>>).
--
|*Proposed Capability* |Rebuild mobile search on the same backend search
service proposed for web (<<ENH-001>> through <<ENH-006>>) rather than a
separate mobile-specific implementation, eliminating the current
--
|*Proposed Capability* |Apply the same server-side capacity enforcement
proposed in <<ENH-015>> uniformly to every mobile entry point, not just the
primary sign-up button.
--
|*Proposed Capability* |Back both mobile apps onto the same
sign-up/commitment API and state model as web (<<CUR-022>>), and give
unvolunteer actions accurate success/failure feedback.
--
|*Proposed Capability* |Render mobile organization/project detail views
from the same data source and templates as web (<<CUR-008>>, <<CUR-010>>) rather
than a parallel mobile-specific rendering path.
--
|*Proposed Capability* |Trace and fix the timezone/formatting divergence
between the mobile search-results renderer and the project-detail
renderer; add this exact scenario to the mobile regression suite given
--
|*Proposed Capability* |Route mobile profile edits and deletion through
the same validated save path as web (<<CUR-036>>, <<CUR-037>>), confirming
persistence rather than assuming client-side success.
--
|*Proposed Capability* |Bring mobile (native app and mobile-web) to full
feature parity with desktop web for calendar integration, success-story
navigation, wizard access, and link sharing, as part of the
--
|*Proposed Capability* |Run a security- and correctness-focused API
audit as an explicit rebuild workstream: input validation and length
limits on all path/query parameters, HTTP status codes that always
--
|*Proposed Capability* |Add an explicit image-consent capture at the
point of photography/upload, a way for a volunteer to find images they
appear in (at minimum via a data-subject request channel, ideally
--
|*Proposed Capability* |Set explicit page-load performance budgets for
the rebuild (see Section 11 NFRs) for the admin console and search, and
profile/fix the specific slow paths identified here as part of that work
