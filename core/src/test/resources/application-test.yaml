datasources:
  default:
    db-type: postgres
    url: jdbc:postgresql://localhost:5432/volunteer_monster
    username: jimmy
    password: warm-farts-smell-worse
    dialect: POSTGRES
    driver-class-name: org.postgresql.Driver